[package]
name = "emu"
version = "0.1.0"
edition = "2024"

[dependencies]
emu-core = { path = "../emu-core" }
sdl2 = { version = "0.38", features = [ "use-pkgconfig" ] }
bytemuck = { version = "1.23", features = [ "derive" ] }
