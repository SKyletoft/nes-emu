[package]
name = "emu-core"
version = "0.0.0"
edition = "2024"

[dependencies]
bitfields = "1.0"
anyhow = "1.0"
derive_more = { version = "2", features = [ "into" ] }
bytemuck = { version = "1.23", features = [ "derive" ] }

[build-dependencies]
cc = "1.2"
