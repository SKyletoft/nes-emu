[package]
name = "emu-core"
version = "0.0.0"
edition = "2024"

[lib]
crate-type = ["lib", "staticlib"]

[dependencies]
anyhow = "1.0"
bitfields = "1.0"
bytemuck = { version = "1.23", features = [ "derive" ] }
derive_more = { version = "2", features = [ "into" ] }

[build-dependencies]
cc = "1.2"
