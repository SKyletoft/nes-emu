[package]
name = "emu-3ds"
version = "0.1.0"
edition = "2024"

[dependencies]
ctru-rs = { git = "https://github.com/rust3ds/ctru-rs" }
emu-core = { path = "../emu-core" }
bytemuck = { version = "1.23", features = [ "derive" ] }
nesc-macro = { path = "../nesc-macro" }

[package.metadata.cargo-3ds]
romfs_dir = "romfs"
